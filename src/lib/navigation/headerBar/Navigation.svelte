<script>
	// I want to import in all the other attibutes
	// I want to leave a spot open for the user to define different attibutes
	// Check to see if the user defined those things
	// If so mount them to the nav bar
	export let heading = null;
	export let link = null;
	export let subHeading = null;
</script>

{#if heading}{/if}
{#if link}{/if}
{#if subHeading}{/if}
